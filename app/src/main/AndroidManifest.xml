<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goldie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Goldieinc">
        <activity
            android:name=".login.Register"
            android:exported="false" />
        <activity
            android:name=".login.Login"
            android:exported="false"
       ></activity>
        <activity
            android:name="com.goldie.menu.menu"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu.ice_cream_menu"/>
        <activity android:name=".menu.crepe_menu"/>
        <activity android:name=".menu.froyo_menu"/>
        <activity android:name=".menu.waffle_menu">
        </activity>

        <activity
            android:name=".menu.menu"
            android:exported="true"></activity>
    </application>

</manifest>